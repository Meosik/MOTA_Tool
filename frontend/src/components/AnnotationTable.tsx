export default function AnnotationTable(){
  return <div style={{border:'1px solid #ddd', padding:8}}>
    <strong>Annotation Table (placeholder)</strong>
    <div>Upload GT/Pred in Runs tab, then edit here (future).</div>
  </div>
}